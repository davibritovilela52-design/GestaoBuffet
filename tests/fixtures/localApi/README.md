# Local API Test Fixture

These modules preserve the old localStorage/mockData behavior for legacy unit tests only.

Production code must use `services/*` and the Supabase-backed service facade, not this fixture.
